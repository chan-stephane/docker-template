version: "3.4"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: template-nextjs-image:latest
    container_name: template-nextjs-container
    ports:
      - 3000:3000
    command: tail -f /dev/null